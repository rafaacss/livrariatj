<form method="post" action="{{ path('app_autor_delete', {'CodAu': autor.CodAu}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autor.CodAu) }}">
    <button class="btn">Delete</button>
</form>
