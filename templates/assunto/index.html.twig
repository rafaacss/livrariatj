{% extends 'base.html.twig' %}

{% block title %}Assunto index{% endblock %}

{% block body %}
    <h1>Assunto index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>CodAs</th>
                <th>Descricao</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for assunto in assuntos %}
            <tr>
                <td>{{ assunto.codAs }}</td>
                <td>{{ assunto.Descricao }}</td>
                <td>
                    <a href="{{ path('app_assunto_show', {'codAs': assunto.codAs}) }}">show</a>
                    <a href="{{ path('app_assunto_edit', {'codAs': assunto.codAs}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_assunto_new') }}">Create new</a>
{% endblock %}
