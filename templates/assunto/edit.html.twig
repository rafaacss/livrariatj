{% extends 'base.html.twig' %}

{% block title %}Editar assunto{% endblock %}

{% block body %}
    <h1>Editar assunto</h1>

    {{ include('assunto/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="justify-content-between align-items-center d-flex gap-2">
        <a href="{{ path('app_assunto_index') }}" class="btn btn-secondary">Voltar</a>

        {{ include('assunto/_delete_form.html.twig') }}
    </div>

{% endblock %}
