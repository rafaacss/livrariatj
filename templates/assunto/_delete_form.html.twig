<form method="post" action="{{ path('app_assunto_delete', {'codAs': assunto.codAs}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assunto.codAs) }}">
    <button class="btn">Delete</button>
</form>
